interface:
  display_name: "Goal Handoff"
  short_description: "Initialize coordinated feature goals"
  default_prompt: "Use $to-goal-handoff to initialize a /goal from this feature execution plan and its ticket graph."
