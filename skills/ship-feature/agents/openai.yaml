interface:
  display_name: "Ship Feature"
  short_description: "Ship a feature end-to-end."
  default_prompt: "Use $ship-feature to ship this scoped feature: delegate implementation, run Codex-owned QA, obtain independent review, address findings, then commit and push."
