interface:
  display_name: "Ship Feature"
  short_description: "Ship a feature end-to-end."
  default_prompt: "Use $ship-feature to implement, fully validate, independently review, and publish this scoped feature."
